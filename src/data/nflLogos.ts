// Intentionally empty: map logos must resolve from bundled local assets only.
// Add NFL team entries here only when they point at local PNG files.
export const nflLogos: Record<string, string> = {};
