// Intentionally empty: school logos must resolve from local assets only.
// Add entries here only when they point at bundled local files, not external URLs.
export const espnLogos: Record<string, string> = {};
